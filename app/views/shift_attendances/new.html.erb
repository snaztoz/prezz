<% content_for :title, "New shift attendance" %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New shift attendance</h1>

  <%= render "form", tenant: @tenant, shift_attendance: @shift_attendance %>

  <%= link_to "Back to shift attendances", tenant_shift_attendances_path(@tenant), class: "w-full sm:w-auto text-center mt-2 sm:mt-0 sm:ml-2 rounded-md px-3.5 py-2.5 bg-gray-100 hover:bg-gray-50 inline-block font-medium" %>
</div>
