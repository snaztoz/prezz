<%= form_with(
  url: @url,
  method: :get,
  class: "w-full",
  data: {
    controller: "submit-on-change",
    submit_on_change_target: "form"
  }) do |f|
%>
  <%= f.select(
    @name,
    @options,
    {
      selected: @selected
    },
    class: "w-full min-h-9 px-4 py-2 border border-neutral-400 text-sm",
    data: {
      action: "submit-on-change#submit"
    })
  %>
<% end %>
